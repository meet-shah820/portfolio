# Optional: production API base (leave empty in dev — Vite proxies /api to localhost:5000)
# VITE_API_URL=https://your-api.example.com

# Must match server ADMIN_API_KEY when set (required for POST/PUT/DELETE projects)
# VITE_ADMIN_KEY=your-secret-key
